// Este arquivo é o service-worker.js
// Por enquanto, ele pode ficar quase vazio,
// apenas para o navegador saber que o app pode ser instalado.

self.addEventListener('install', (event) => {
  // Evento de instalação
});

self.addEventListener('fetch', (event) => {
  // Evento de busca (não faz nada, por enquanto)
});